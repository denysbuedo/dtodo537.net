# Tenant isolation tests

M2 starts tenant isolation coverage in `apps/api/src/modules/tenancy`.

Future tenant-aware modules must add cross-tenant tests here or in their package-level suites and keep this directory as the security review index.
